{
  "config": {
    "step": {
      "user": {
        "title": "Default Config Manager",
        "description": "Set up the Default Config Manager integration."
      }
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Default Config Manager",
        "description": "**Status:** {mode_display}\n**HA Core Version:** {default_config_version}\n\n{status_message}"
      }
    }
  },
  "issues": {
    "default_config_change": {
      "title": "Default Config integration changed",
      "description": "The integration '{component}' was {action} by Default Config Manager."
    }
  }
}
